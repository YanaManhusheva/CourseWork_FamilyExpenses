@model IEnumerable<FamilyExpensesCourseWork.Models.vExpense>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cFamilyMember.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cFamilyMember.сFamily.Surname)
        </th>
        <th>
            @Html.DisplayName("Expense Sum")
        </th>
        <th>
            @Html.DisplayName("Category")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cFamilyMember.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cFamilyMember.сFamily.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpenseSum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cExpensePurpose.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
